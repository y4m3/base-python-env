# base-python-env

python 3.9.4 environment with docker and poetry

## Usage

### Build and Start Docker Env

```bash
docker-compose up -d --build
```

### Stop Docker Env

```bash
docker-compose down
```
